<UserControl x:Class="CC.UserControls.Notificacoes"
              xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CC.UserControls"
             xmlns:Icon="http://metro.mahapps.com/winfx/xaml/iconpacks"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="900">
    <Grid>
        <DataGrid Background="#eff2f7" Style="{DynamicResource DataGridStyle1}" CellStyle="{DynamicResource DataGridCellStyle1}" ColumnHeaderStyle="{DynamicResource DataGridColumnHeaderStyle1}" RowStyle="{DynamicResource DataGridRowStyle2}" Grid.Row="5" x:Name="GridNotificacoes">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Assunto" IsReadOnly="True" Width="*" Binding="{Binding Titulo}" />
                <DataGridTextColumn Header="Mensagem" IsReadOnly="True" Width="*" Binding="{Binding Mensagem}" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
